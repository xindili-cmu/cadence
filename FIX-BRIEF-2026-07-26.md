# 数据质量修复 · 2026-07-26

来源:对 origin/main、线上中英两版、news.json 与 W30 周报的复查。**代码已改完,数据修复脚本已写好并测通,剩下需要你在本地跑**(沙箱无外网,Crossref 和 og:title 都取不到)。

---

## 已改(在 repo 里)

### 1. `sources.json` — 三个 Springer 源加 DOI path 约束

根因:`news-refresh.js` 的 `matchSource()` 按 hostname 后缀匹配,三个 Springer 源的 domain 都是裸的 `link.springer.com`,于是 Exa 抓到的**任意** springer/BMC 文章都命中排在最前的「Sports Medicine」。OUP 源早有 path 约束先例(`academic.oup.com/ptj`),Springer 漏了。

影响面比初판估计大:归档里 **196 行**受影响、横跨约 **87 本刊**,**无一行**真属 Sports Medicine(没有一条带 s40279 前缀)。

```
link.springer.com  →  link.springer.com/article/10.1007/s40279   (Sports Medicine)
                      link.springer.com/article/10.1007/s00586   (Eur Spine J)
                      link.springer.com/article/10.1007/s12178   (Curr Rev Musculoskelet Med)
```

已验证:`matchSource()` 现在把 `10.1186/s12951` 判为 off-roster(null),`10.1007/s40279` 仍判 Sports Medicine。

### 2. `scripts/news-refresh.js` — 三道入库闸门

- **`isJunkUrl()`**:career/job 子域与路径、login/cart/search 页。在 curation **之前**过滤,省 LLM 调用。
- **`isJunkItem()`**:空标题 / 截断标题 / 标题就是刊名(roster 精确匹配 + `looksLikeJournalLanding()` 通用启发式)/ summary 含导航残渣(`the the`、`Visit the dedicated webpage` 等)。
- **`repairTruncatedTitles()`**:入库前抓 og:title 补全截断标题,补不回来的由 `isJunkItem` 丢弃。放在 curation 前,所以 LLM 拿到的是完整标题。

9 条标题用例全过(`Journal of orthopaedic surgery and research` 判 junk;`Journal of neuroengineering and rehabilitation: a 20-year review` 判正常)。

### 3. `scripts/fix-source-attribution.js` — 新增,历史数据修复

幂等(已用 fixture 验证两次跑后 news.json 逐字节一致),复用 news-refresh 导出的 `isJunkUrl/isJunkItem/isTruncatedTitle`,一份定义、历史与新数据口径一致。

**roster 即白名单**:springer 行的 DOI 前缀能映射到 sources.json 条目才保留(并把 source/journal 改成真实刊名),否则丢弃 —— 与修好后的管线行为一致。要救哪本刊,加一条 sources.json 就行,不用改代码。

同时处理 related 子条目(它们也带 source 标签、也会渲染)、截断标题(BMJ 的 `.full.pdf` 会自动回退到去掉后缀的 HTML 页取 og:title)、以及垃圾行;最后重建 `archive/index.json`。

---

## 你要跑的(按顺序)

```bash
cd ~/Desktop/"healthcare future"
rm -f .git/index.lock && git pull --no-rebase     # 本地落后 origin ~90 提交
```

### ① 先看报告,再决定救哪些刊

```bash
node scripts/fix-source-attribution.js --report
```

会打印所有 off-roster 的 Springer 刊(Crossref 查真实刊名),按行数排序。**这一步不写任何文件。**

按 7-26 的本地数据估算,会被丢弃的大户里有这些明显跑题的:Molecular Cancer、Acta Neuropathologica、J Nanobiotechnology、BMC Nursing。但也有几个前缀量很大、值得你确认是不是真·康复刊(报告会给出准确刊名,我在沙箱查不到,不猜):

| 前缀 | 行数 |
| --- | ---: |
| 10.1186/s12877 | 15 |
| 10.1186/s12891 | 13 |
| 10.1186/s13102 | 11 |
| 10.1186/s12984 | 9 |
| 10.1186/s13018 | 7 |
| 10.1186/s40798 | 5 |

(唯一我这边核实过的:`10.1186/s12984` = *Journal of NeuroEngineering and Rehabilitation*,Crossref 单条查询确认。)

想留哪本,就往 sources.json 加一条,然后重跑报告:

```json
{
  "name": "J NeuroEng Rehabil",
  "domain": "link.springer.com/article/10.1186/s12984",
  "url": "https://link.springer.com/journal/12984",
  "kind": "journal",
  "journalName": "Journal of NeuroEngineering and Rehabilitation",
  "regions": ["Global"],
  "cats": ["neurological"]
}
```

### ② 应用

```bash
DRY_RUN=true node scripts/fix-source-attribution.js   # 先空跑看数
node scripts/fix-source-attribution.js                # 真跑
```

### ③ 补中文字段

脚本会把「标题恢复了但 titleZh 还带…」的行的 titleZh/summaryZh 删掉。另外当前 news.json 本来就有 6 行缺 titleZh/summaryZh。两件事一起补:

```bash
node scripts/backfill-i18n.js     # 生产用 gemini,确认 LLM_PROVIDER=gemini
node scripts/lint-daily.js        # 应无 missing titleZh/summaryZh
```

### ④ 验收

```bash
node -e "
const fs=require('fs');
const files=['news.json',...fs.readdirSync('archive').filter(f=>/^\d{4}-\d{2}\.json$/.test(f)).map(f=>'archive/'+f)];
let bad=0;
for(const f of files){for(const i of JSON.parse(fs.readFileSync(f,'utf8')).items||[]){
  if(i.source==='Sports Medicine'&&!/10\.1007\/s40279/.test(i.sourceUrl))console.log('❌ 错标',f,i.title.slice(0,50)),bad++;
  if(/(\.\.\.|…)\s*\$/.test(i.title||'')||/(\.\.\.|…)\s*\$/.test(i.titleZh||''))console.log('❌ 截断',f,i.title.slice(0,50)),bad++;
  if(!String(i.title||'').trim())console.log('❌ 空标题',f),bad++;
}}
console.log(bad?bad+' 项待修':'✅ 全部通过');
"
```

线上再抽查:头条与 AI 速读无「..」;卡片刊名与 sourceUrl 一致。下一期周报来源构成不该再出现「Sports Medicine · 66」。

**不用重建 app.min.js** —— 这次没动前端组件。

---

## 没做的(等你定)

- **分数压缩**(65–75 占 58/75,周报强信号 18→8):主因就是 Springer 洪流稀释。打分 rubric(90/80)和展示档(85/75/65)都没动 —— 先跑完上面的修复,看下周分布再说。
- **news 类打分口径**:AHPRA「假足病师判刑」评了 90(rubric 里 90 = 可改变实践),news 与 research 的打分心智可能不一致。要不要给 news 单独封顶,得你先看一眼近四周 news 类的分数分布。
- **7 行缺 limitation**(多为 practice/news 类):如果是有意为之,前端加兜底文案即可。注意改组件只能改 `design-system/components/**` 真源,不能直接改 `components.bundle.jsx`。
- **盆底类归类**:女性尿失禁手术被归「骨科」,量上来了再单独开一类。
