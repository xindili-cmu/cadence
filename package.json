{
  "name": "cadence-pt-news",
  "version": "1.0.0",
  "description": "Daily auto-refresh for Cadence — physical therapy news feed",
  "private": true,
  "scripts": {
    "refresh": "node scripts/news-refresh.js",
    "test-refresh": "DRY_RUN=true node scripts/news-refresh.js"
  },
  "engines": {
    "node": ">=20"
  }
}
