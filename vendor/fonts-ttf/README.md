# vendor/fonts-ttf

TTF copies of the brand web fonts (IBM Plex Sans, IBM Plex Mono, Spectral),
converted from the woff2 originals in `../fonts/`. satori (used by
`scripts/linkedin-card.js`) can't read woff2, so it reads these TTFs instead.
Latin subset only — same files, different container.
